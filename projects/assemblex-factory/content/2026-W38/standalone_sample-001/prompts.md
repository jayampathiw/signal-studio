# standalone_sample-001 — prompts

**Status:** placeholder — this post was scaffolded for P0.8-01, not produced by the real master prompt.

Replace this file with the actual prompt transcript once the master prompt is run in Mode C:
- the exact prompt(s) sent to Claude to produce `pack.json`
- the resulting raw output (before any hand-editing)
- the Google Flow prompts used per shot (for `production-log.csv`)

## Assumed content (for scaffolding only)

Subject: mimic octopus, 3 shots, standalone (Mode C) post, outputs `fb` + `ig`.
